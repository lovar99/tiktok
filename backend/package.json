{
  "name": "tiktok-app",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "build": "esbuild server.js --bundle --platform=node --packages=external --outfile=build.js && javascript-obfuscator build.js --output dist-server.js --target node --string-array true --dead-code-injection true",
    "start": "node dist-server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "esbuild": "^0.28.1",
    "express": "^4.19.2",
    "javascript-obfuscator": "^5.5.0",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.8.3",
    "tiktok-live-connector": "^2.4.2"
  }
}
